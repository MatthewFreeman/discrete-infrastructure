# Discrete servers are intentionally IPv4-only.
# Disable IPv6 on every existing interface, on loopback, and by default
# for interfaces created after this policy is loaded.
net.ipv6.conf.all.disable_ipv6 = 1
net.ipv6.conf.default.disable_ipv6 = 1
net.ipv6.conf.lo.disable_ipv6 = 1
