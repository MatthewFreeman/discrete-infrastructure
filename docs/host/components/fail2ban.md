# Fail2Ban

The host module deploys and verifies the repository-managed Fail2Ban configuration using nftables
with IPv4-only table behavior. Canonical configuration is under
[`modules/host/configs/fail2ban/`](../../../modules/host/configs/fail2ban/).
